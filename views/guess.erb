<h1><%= $game.display_word %><h1>
<h2> <%= $game.guessed %> </h2>
<h3> <%= @lives > 1 ? "#{@lives} lives" : "#{@lives} life" %> left </h3>
<form method= "POST" action= "http://localhost:4567/guess">
  <% @letters.each do |letter| %>
  <input name = "letter" type="submit" value=<%=letter%> />
  <% end %>
</form>
